import 'draw_card.dart';


void InitialDeal(List<int> playerHand, List<int> houseHand, List<int> deck){
  for(int i = 0; i < 2; i++){
    DrawCard(List<int> playerHand, List<int> deck);
    DrawCard(List<int> houseHand, List<int> deck);
  }
}